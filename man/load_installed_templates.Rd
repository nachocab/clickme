\name{load_installed_templates}
\alias{load_installed_templates}
\title{This function load the R source files in templates/<Chart>/R
It first checks that those files are not polluting the global namespace. They are only allowed to define methods to their <Chart> object}
\usage{
  load_installed_templates()
}
\description{
  This function load the R source files in
  templates/<Chart>/R It first checks that those files
  are not polluting the global namespace. They are only
  allowed to define methods to their <Chart> object
}

